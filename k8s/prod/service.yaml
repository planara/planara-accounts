apiVersion: v1
kind: Service
metadata:
  name: svc-accounts
  namespace: planara
spec:
  type: ClusterIP
  selector:
    app: svc-accounts
  ports:
    - name: http
      port: 8080
      targetPort: 8080

    - name: management
      port: 8081
      targetPort: 8081